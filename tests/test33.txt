class Test
{
	static int a,b, Test;
    static boolean[] c,d;

    public static int[] banana(boolean a, int b, int c2) {        
        a = !(true && c[2]);
        System.out.println(c[2]);
        return (new int[2]);
    }

    public static int to_int(boolean unused) { return 2; }
    public static boolean to_bool(int unused) { return true; }

    public static void main(String[] a)
    {
        int c,d;
        int[] e;
        boolean f;
        c = e[3] + e.length;
        c = 2+3+4/(5+2) + (new int[2])[0] % -e[3] + e.length;
        c = b + 2;
        c = d % 3 + c / 3;

        if ( true ) {
            if ( !(new boolean[2])[0] )
                System.out.println(d);
        } else {
            while(true && 2) c = 2;
            return ;
        }


        f = (false) || (!!!!false) && false;

        e[2] = to_int((new boolean[banana(true,2,banana(true,0,0)[1])[1]])[3]) / 2 + 3* to_int(to_bool(e[2]) && true);
        /*c = banana(true,2,3);*/

    }
    
}
