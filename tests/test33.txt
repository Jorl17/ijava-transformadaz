class Test
{
	static int a,b, Test;
    static boolean[] c,d;

    public static int banana(boolean a) {        
        a = !(true && c[2]);
        System.out.println(c[2]);
        return (new int[2])[1];
    }

    public static void main(String[] a)
    {
        int c,d;
        int[] e;
        boolean f;
        c = e[3] + e.length;
        c = 2+3+4/(5+2) + (new int[2])[0] % -e[3] + e.length;
        c = b + 2;
        c = d % 3 + c / 3;

        if ( true ) {
            if ( !(new boolean[2])[0] )
                System.out.println(d);
        } else {
            while(true && 2) c = 2;
            return ;
        }


        f = (false) || (!!!!false) && false;

        a[3] = Integer.parseInt(a[2]);

    }
    
}
